#include "queue.h"
#include "reverse.h"
#include "circular_queue.h"
#include <stdio.h>

int main(){
  // Queue* queue = make_queue();
  // int elements[5] = {1,2,3,4,5};
  // for(int i = 0; i < 5; i++){
    
  //   printf("Deb@ %d v: %d\n", i, *(int*)(&elements[i]));
  //   queue_enqueue(queue, (int*)(&elements[i]));
  // }
  // Stack* stack = reverse_queue(queue);
  // StackItem* item = stack->head;
  // int val = *(int*)item->data;
  // printf("Stack@ %d v: %d\n", 0, val);
  // for(int i = 1; i < stack->len; i++){
  //   item = (StackItem*)item->next_item;
  //   val = *(int*)item->data;
  //   printf("Stack@ %d v: %d\n", i, val);
  // }
  // CircularQueue* queue = make_circular_queue();
  // int elements[5] = {1,2,3,4,5};
  // for(int i = 0; i < 5; i++){
    
  //   printf("Deb@ %d v: %d\n", i, *(int*)(&elements[i]));
  //   circular_queue_enqueue(queue, (int*)(&elements[i]));
  // }
  // CircularQueueElement* cur_elem = queue->head->next_element;
  // for(int i = 0; i < 100; i++){
  //   int val = *(int*)cur_elem->content;
  //   printf("Queue Element: %d Value: %d\n", i , val );
  //   cur_elem = cur_elem->next_element;
  // }
}